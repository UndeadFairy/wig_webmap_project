{
  "name": "accessible",
  "dependencies": {
    "ol": "6.5.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build src/main.js --dist-dir static/dist",
    "test": "parcel build --help",
    "watch": "parcel watch src/main.js --dist-dir static/dist"
  }
}
